# Files created by the pipeline, which we want to keep out of git
# (or at least out of _this_ git repo).
data/
results/
auspice/
build/
outbreak_data/
logs/

# Keep ingest/data directory, but ignore all files within it
!ingest/data/
ingest/data/*
# Explicitly include sequences that we want to add to the automated ingest
!ingest/data/Finland-MPX-37+42.ndjson

# Sensitive environment variables
environment*
env.d/

# Snakemake state dir
.snakemake

# Local config overrides
/config_local.yaml

# For Python #
##############
*.pyc
.tox/
.cache/

# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?
ehthumbs.db
Thumbs.db
